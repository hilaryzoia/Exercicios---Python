# A student's grade point average

n1 = float(input('First note: '))
n2 = float(input('Second note: '))
m = (n1+n2)/2
print('The average between the grades {:.1f} and {:.1f} is {:.1f}'.format(n1,n2,m))
